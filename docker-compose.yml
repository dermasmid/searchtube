version: "3.7"


services:
    wsgi:
        build: ./
        container_name: "${PROJECT_NAME}_wsgi"
        volumes:
            - ./web:/var/www/gotube/web
            - ./.data/channels:/var/www/gotube/data
            - ./gotube:/usr/local/lib/python3.7/site-packages/gotube
            - ./tests:/tests
        ports:
            - "80:80"
            - "443:443"
        env_file:
                - .env

    mongo:
        image: mongo
        container_name: "${PROJECT_NAME}_mongo"
        environment:
            MONGO_INITDB_ROOT_USERNAME: $DB_USERNAME
            MONGO_INITDB_ROOT_PASSWORD: $DB_PASSWORD
        volumes:
            - ./.data/db:/data/db:delegated
        ports:
            - "27017:27017"
